{
  "name": "server",
  "version": "1.0.0",
  "description": "Plantshop backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../client/ && npm run build && cp -r build ../server"
  },
  "author": "Juha Anttila",
  "license": "ISC",
  "dependencies": {
    "@apollo/client": "^3.6.9",
    "apollo-server": "^3.10.0",
    "dotenv": "^16.0.1",
    "graphql": "^16.5.0",
    "mongoose": "^6.4.5"
  }
}
